version: '3.8'

services:
  scrapeflow:
    build: .
    ports:
      - "3000:3000"
    restart: unless-stopped
    volumes:
      - ./backend/.jobs:/app/backend/.jobs
      - ./resultados:/app/resultados
    environment:
      - NODE_ENV=production
